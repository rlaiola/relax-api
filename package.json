{
  "name": "relax_api",
  "version": "1.0.0",
  "description": "This application implements a workaround to return only the data (json) from a RelaX query API request",
  "main": "index.js",
  "scripts": {
    "serve": "node server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "RelaX",
    "Relational Algebra",
    "Database systems"
  ],
  "author": "Rodrigo Laiola Guimaraes",
  "license": "GPL-3.0",
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.1",
    "puppeteer": "^13.7.0",
    "winston": "^3.3.3"
  }
}
